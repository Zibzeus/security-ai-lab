# Trusted Knowledge

Place approved SOPs, playbooks, architecture notes, control mappings, and sanitized
incident reports in this directory as Markdown files. Reindex with:

`POST /v1/knowledge/reindex`

Retrieved content is treated as evidence, never as executable instructions.
